# Integrations archivado

La carpeta `integrations` ya no contiene código activo del producto.

- El cálculo de flexografía activo vive en `public/calculo-flexografia/`.
- El motor/backend de flexografía vive en `services/flexo-engine/`.
- Los servicios SAP y tipo de cambio viven en `services/`.
- El contenido anterior se movió a `_legacy/` durante la unificación.
